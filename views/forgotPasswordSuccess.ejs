<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <meta name="author" content="Abradu Frimpong Kwame" />
    <meta name="description" content="An Examination Website" />
    <meta name="keywords" content="Examination" />

    <link rel="icon" href="/images/favicon.ico" type="image/x-icon" />

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous" />

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" crossorigin="anonymous" />

    <!-- AOS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.css" crossorigin="anonymous" />
    
    <title>Reset Password</title>

    <style>
      .password-toggle {
        position: absolute;
        right: 1rem;
        top: 75%;
        transform: translateY(-50%);
        cursor: pointer;
        color: #6c757d;
      }

      .form-group {
        position: relative;
        margin-bottom: 1.5rem;
      }

      .headerText {
        text-align: center;
        margin-bottom: 1.5rem;
      }
      .alert{
        font-size: 2rem;
      }
    </style>
  </head>
  <body>
    <main role="main">
      <section class="password-reset d-flex justify-content-center align-items-center min-vh-100" id="password-reset">
        <div class="container">
          <div class="row justify-content-center">
            <div class="col-md-6">
              <div class="card shadow-sm">
                <div class="card-body">
                  
                  
                  <% if (error) { %>
                    <div class="alert alert-danger mt-3 text-center" role="alert"><%= error %></div>
                  <% } %>

                  <% if (success) { %>
                    <div class="alert alert-success mt-3 text-center" role="alert"><%= success %></div>
                  <% } %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>
    </main>
  </body>
</html>
