    <footer class="mt-auto bg-primary text-light text-center py-3">
      <div class="container">
        <p class="mb-0">&copy; <%= new Date().getFullYear() %> Exams Portal. All rights reserved.</p>
      </div>
    </footer>

    <!-- Bootstrap JS (necessary for responsive navbar) -->
    <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>

    <!-- OR use CDN if local doesn't work -->
    <!--
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-qRj7oS+jYI0PUA3aR8AHHjUhMDZLHZsWq2MEcdEY3qvlCyk6ZzYFtxE5CWgkT1aC"
      crossorigin="anonymous"></script>
    -->

    <script>
      // Auto-hide error message after 2 seconds
      const errorMessage = document.getElementById('error-message');
      if (errorMessage) {
        setTimeout(() => {
          errorMessage.style.display = 'none';
        }, 2000);
      }
    </script>
    <script>
      document.addEventListener("DOMContentLoaded", () => {
        // ✅ Hide error message
        const errorMessage = document.getElementById("error-message");
        if (errorMessage) {
          setTimeout(() => {
            errorMessage.style.display = "none";
          }, 2000);
        }

        // ✅ Toggle main password visibility
        const togglePassword = document.getElementById("togglePassword");
        const passwordInput = document.getElementById("password");
        if (togglePassword && passwordInput) {
          togglePassword.addEventListener("click", () => {
            const type = passwordInput.getAttribute("type") === "password" ? "text" : "password";
            passwordInput.setAttribute("type", type);
            togglePassword.innerHTML =
              type === "password"
                ? '<i class="fas fa-eye"></i>'
                : '<i class="fas fa-eye-slash"></i>';
          });
        }

        // ✅ Toggle confirm password visibility
        const togglePassword1 = document.getElementById("togglePassword1");
        const passwordInput1 = document.getElementById("confirmPassword");
        if (togglePassword1 && passwordInput1) {
          togglePassword1.addEventListener("click", () => {
            const type = passwordInput1.getAttribute("type") === "password" ? "text" : "password";
            passwordInput1.setAttribute("type", type);
            togglePassword1.innerHTML =
              type === "password"
                ? '<i class="fas fa-eye"></i>'
                : '<i class="fas fa-eye-slash"></i>';
          });
        }

        // ✅ Alert cleanup
        const alertMessage = document.querySelector(".alert-danger");
        if (alertMessage) {
          setTimeout(() => {
            alertMessage.textContent = "";
            alertMessage.classList.remove("alert");
          }, 3000);
        }
      });
    </script>

  </body>
</html>
